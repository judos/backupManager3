check problems with files which could not get copied
use front slashes everywhere in logs
show "open log" button when backup is finished
store log locally in temp.txt instead of OS.temp folder

bundle release as jar and publish